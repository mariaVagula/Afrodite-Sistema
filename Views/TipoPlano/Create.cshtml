@model Afrodite_Sistema.Models.TipoPlano

@{
    ViewData["Title"] = "Create";
}

<h1>Cadastrar</h1>

<h4>Tipo do Plano</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NomeTipoPlano" class="control-label">Tipo do Plano</label>
                <input asp-for="NomeTipoPlano" class="form-control" />
                <span asp-validation-for="NomeTipoPlano" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Cadastrar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-primary">Voltar à Lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
