@model IEnumerable<Afrodite_Sistema.Models.TipoPlano>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoPlano)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipoPlano)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoPlanoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoPlanoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoPlanoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
