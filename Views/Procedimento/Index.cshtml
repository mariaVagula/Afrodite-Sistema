@model IEnumerable<Afrodite_Sistema.Models.Procedimento>

@{
    ViewData["Title"] = "Index";
}

<h1>Procedimento</h1>

<p>
    <a asp-action="Create">Cadastrar Procedimento</a>
</p>
<div>
    @using (Html.BeginForm("Index", "Procedimento", FormMethod.Get))
    {
        <p>
            Digite aqui sua pesquisa: @Html.TextBox("pesquisa")
            <input type="submit" value="Pesquisar"/>
        </p>
    }
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeProcedimento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValorProcedimento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeProcedimento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorProcedimento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProcedimentoId">Editar</a> 
                <a asp-action="Details" asp-route-id="@item.ProcedimentoId">Detalhes</a> 
                <a asp-action="Delete" asp-route-id="@item.ProcedimentoId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
