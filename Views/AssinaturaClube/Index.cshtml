@model IEnumerable<Afrodite_Sistema.Models.AssinaturaClube>

@{
    ViewData["Title"] = "Index";
}

<h1>Assinatura do Clube</h1>

<p>
    <a asp-action="Create">Cadastrar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clube)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.NomeCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clube.NomeClube)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AssinaturaClubeId">Editar</a> 
                <a asp-action="Details" asp-route-id="@item.AssinaturaClubeId">Detalhes</a> 
                <a asp-action="Delete" asp-route-id="@item.AssinaturaClubeId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
