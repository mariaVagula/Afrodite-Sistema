@model IEnumerable<Afrodite_Sistema.Models.AssinaturaClube>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clube)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.ClienteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clube.ClubeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AssinaturaClubeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AssinaturaClubeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AssinaturaClubeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
