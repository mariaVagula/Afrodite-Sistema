@model IEnumerable<Afrodite_Sistema.Models.TipoClube>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoClube)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipoClube)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoClubeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoClubeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoClubeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
