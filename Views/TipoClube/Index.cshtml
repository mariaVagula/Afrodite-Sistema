@model IEnumerable<Afrodite_Sistema.Models.TipoClube>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipo do Clube</h1>

<p>
    <a asp-action="Create" class="cadastrar">Cadastrar Tipo do Clube</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoClube)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipoClube)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoClubeId" class="btn btn-primary">Editar</a> 
                    <a asp-action="Details" asp-route-id="@item.TipoClubeId" class="btn btn-primary">Detalhes</a>
                    <a asp-action="Delete" asp-route-id="@item.TipoClubeId" class="btn btn-segundo">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
