@model Afrodite_Sistema.Models.PagamentoAssinatura

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>PagamentoAssinatura</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PagamentoAssinaturaId" />
            <div class="form-group">
                <label asp-for="AssinaturaClubeId" class="control-label"></label>
                <select asp-for="AssinaturaClubeId" class="form-control" asp-items="ViewBag.AssinaturaClubeId"></select>
                <span asp-validation-for="AssinaturaClubeId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FormaPagamentoId" class="control-label"></label>
                <select asp-for="FormaPagamentoId" class="form-control" asp-items="ViewBag.FormaPagamentoId"></select>
                <span asp-validation-for="FormaPagamentoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
