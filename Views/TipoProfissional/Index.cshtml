@model IEnumerable<Afrodite_Sistema.Models.TipoProfissional>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoProfissional)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipoProfissional)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoProfissionalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoProfissionalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoProfissionalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
