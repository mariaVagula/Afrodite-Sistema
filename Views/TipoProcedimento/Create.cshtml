@model Afrodite_Sistema.Models.TipoProcedimento

@{
    ViewData["Title"] = "Create";
}

<h1>Cadastrar</h1>

<h4>Tipo do Procedimento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NomeTipoProcedimento" class="control-label"></label>
                <input asp-for="NomeTipoProcedimento" class="form-control" />
                <span asp-validation-for="NomeTipoProcedimento" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Cadastrar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-primary">Voltar à Lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
